post:
  operationId: pagarTransaccionPendiente
  summary: Pagar el saldo de una transacción pendiente
  tags:
    - paciente_acciones
  parameters:
    - $ref: "../../../components/parameters.yaml#/id_transaccion_path"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            metodo_pago_id:
              type: string
              example: "card_9876"
  responses:
    "200":
      description: Pago completado.
      content:
        application/json:
          schema:
            type: object
            properties:
              nuevo_estado:
                type: string
                example: "PAGADO"
    "400":
      $ref: "../../../components/responses.yaml#/PeticionIncorrecta"
    "401":
      $ref: "../../../components/responses.yaml#/NoAutorizado"
